name: cpm-hello-world
tag: latest
source: https://github.com/acf-patrick/cpm-hello-world.git

build:
  - steps: gcc main.c -o hello_world

run:
  - os: windows
    arch: x86_64
    steps: |
      echo running on windows
      ./hello_world
  - os: linux
    steps: |
      echo running on linux
      ./hello_world
    